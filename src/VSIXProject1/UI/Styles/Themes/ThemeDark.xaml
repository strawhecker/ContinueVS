<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- VSCode Dark Theme Color Palette -->

    <!-- Editor and Panel Backgrounds -->
    <SolidColorBrush x:Key="EditorBackground" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="PanelBackground" Color="#252526"/>
    <SolidColorBrush x:Key="CodeBackground" Color="#2D2D30"/>
    <SolidColorBrush x:Key="SidebarBackground" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="DockPaneBackground" Color="#252526"/>

    <!-- Text Colors -->
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="#E0E0E0"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#858585"/>
    <SolidColorBrush x:Key="TertiaryTextBrush" Color="#6A6A6A"/>
    <SolidColorBrush x:Key="DisabledTextBrush" Color="#6F6F6F"/>

    <!-- Accent Colors -->
    <SolidColorBrush x:Key="AccentBrush" Color="#007ACC"/>
    <SolidColorBrush x:Key="ButtonPrimaryBrush" Color="#0E639C"/>
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#1177BB"/>
    <SolidColorBrush x:Key="ButtonActiveBrush" Color="#094771"/>
    <SolidColorBrush x:Key="FocusBrush" Color="#007ACC"/>

    <!-- Status Colors -->
    <SolidColorBrush x:Key="SuccessBrush" Color="#13C127"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#DCA81B"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#F14C4C"/>
    <SolidColorBrush x:Key="InfoBrush" Color="#007ACC"/>

    <!-- Borders and Separators -->
    <SolidColorBrush x:Key="BorderBrush" Color="#464647"/>
    <SolidColorBrush x:Key="DarkBorderBrush" Color="#3E3E42"/>
    <SolidColorBrush x:Key="LightBorderBrush" Color="#555555"/>

    <!-- Selection and Highlight -->
    <SolidColorBrush x:Key="SelectionBrush" Color="#094771"/>
    <SolidColorBrush x:Key="HighlightBrush" Color="#3E3E42"/>
    <SolidColorBrush x:Key="HoverBrush" Color="#2D2D30"/>

    <!-- Semantic UI Colors (for specific components) -->
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="#0E639C"/>
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="#3C3C3C"/>
    <SolidColorBrush x:Key="InputBorderBrush" Color="#464647"/>
    <SolidColorBrush x:Key="ModalOverlayBrush" Color="#000000"/>

    <!-- Opacity for overlays (used via SolidColorBrush with Opacity property) -->
    <SolidColorBrush x:Key="ModalOverlayTransparent" Color="#000000" Opacity="0.5"/>

    <!-- Default TextBox Style -->
    <Style x:Key="DefaultTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- RadioButton Style -->
    <Style x:Key="ThemedRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="16" Height="16" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8" Background="{TemplateBinding Background}">
                            <Ellipse x:Name="CheckEllipse" Fill="{StaticResource AccentBrush}" Visibility="Collapsed" Margin="3"/>
                        </Border>
                        <TextBlock Grid.Column="1" Margin="6,0,0,0" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}" Text="{TemplateBinding Content}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckEllipse" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Item Style -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#252526"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <TextBlock Text="{TemplateBinding Content}" Foreground="White" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#3E3E42"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#094771"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Style -->
    <Style x:Key="ThemedComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#1E1E1E"/>
        <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Grid.ColumnSpan="2" Background="#1E1E1E" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"/>
                        <TextBlock Grid.Column="0" Text="{Binding SelectedItem.Name, RelativeSource={RelativeSource TemplatedParent}}" Foreground="White" Padding="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Background="Transparent" BorderThickness="0" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center">▼</Button>
                        <Popup IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Bottom" AllowsTransparency="False">
                            <Border Background="#252526" BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="1">
                                <ScrollViewer MaxHeight="200">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Button Style -->
    <Style x:Key="ThemedButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Background" Value="{StaticResource ButtonPrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2" Padding="{TemplateBinding Padding}">
                        <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Expander Style -->
    <Style x:Key="ThemedExpanderStyle" TargetType="Expander">
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource PanelBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>

</ResourceDictionary>
