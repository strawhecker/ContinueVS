<Window x:Class="ContinueVS.UI.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Progress" Height="120" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0"
        ShowActivated="False"
        ShowInTaskbar="False"
        Visibility="Hidden">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Name="ProgressLabel" Text="Processing..." Foreground="#333333" Margin="0,0,0,10" FontSize="14" Grid.Row="0"/>
        <ProgressBar Name="ProgressBar" Height="20" Background="#E0E0E0" Foreground="#0078D4" Margin="0,0,0,10" Grid.Row="1"/>
        <TextBlock Name="PercentLabel" Text="0%" TextAlignment="Right" Foreground="#666666" FontSize="12" Grid.Row="2"/>
    </Grid>
</Window>
