<Window x:Class="ContinueVS.UI.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="180" Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource EditorBackground}"
        ShowInTaskbar="True">
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="20">
        <StackPanel VerticalAlignment="Stretch">
            <TextBlock x:Name="PromptLabel" Text="Enter value:" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,10" FontSize="14" TextWrapping="Wrap"/>
            <TextBox x:Name="InputTextBox" Height="32" Padding="8" Margin="0,0,0,20" FontSize="13" VerticalContentAlignment="Center" Style="{StaticResource DefaultTextBoxStyle}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Height="36">
                <Button x:Name="OkButton" Content="OK" Width="80" Margin="0,0,10,0" Click="OkButton_Click" 
                        Background="{StaticResource ButtonPrimaryBrush}" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                <Button x:Name="CancelButton" Content="Cancel" Width="80" 
                        Background="{StaticResource ErrorBrush}" Foreground="White" FontSize="12" VerticalAlignment="Center" Click="CancelButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
