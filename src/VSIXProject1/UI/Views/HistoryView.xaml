<UserControl x:Class="ContinueVS.UI.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ContinueVS.UI.Views">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="SessionItemStyle" TargetType="{x:Type ListBoxItem}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="Margin" Value="0,2"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                            <Border Name="Bd" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="#3E3E42"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#2D2D30"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" CornerRadius="4">
        <DockPanel>
            <!-- Header -->
            <Border DockPanel.Dock="Top" Background="#252526" BorderBrush="#3E3E42" BorderThickness="0,0,0,1" Padding="12,8">
                <StackPanel>
                    <TextBlock Text="Session History" FontWeight="Bold" Foreground="White" FontSize="14"/>
                    <TextBlock Text="{Binding AvailableSessions.Count, StringFormat='Available: {0} sessions'}" 
                               Foreground="#A0A0A0" FontSize="11" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Sessions List -->
            <ListBox Name="SessionsListBox"
                     ItemsSource="{Binding AvailableSessions}"
                     SelectionMode="Single"
                     Background="#1E1E1E"
                     Foreground="White"
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     Padding="8">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <!-- Session Title -->
                            <TextBlock Text="{Binding Title}" 
                                       FontWeight="Bold" 
                                       Foreground="White"
                                       TextTrimming="CharacterEllipsis"/>

                            <!-- Session Metadata -->
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock Text="{Binding UpdatedAt, StringFormat='{}Last: {0:g}'}" 
                                           Foreground="#A0A0A0" 
                                           FontSize="11"
                                           Margin="0,0,12,0"/>
                                <TextBlock Foreground="#A0A0A0" FontSize="11">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0} messages">
                                            <Binding Path="MessageCount"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource SessionItemStyle}"/>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </DockPanel>
    </Border>
</UserControl>
